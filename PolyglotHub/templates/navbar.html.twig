<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><i class="fas fa-language me-2"></i>PolyGlotHub</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse container-fluid" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item me-3">
                    <a class="nav-link" href="{{ path('app_projects') }}">
                        <i class="fas fa-project-diagram me-1"></i> Projets
                    </a>
                </li>
                <li class="nav-item me-3">
                    <a class="nav-link" href="{{ path('app_dashboard') }}">
                        <i class="fas fa-chart-line me-1"></i> Dashboard
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item me-3">
                    <a class="nav-link" href="{{ path('app_profile') }}">
                        <i class="fas fa-user me-1"></i> Mon profil
                    </a>
                </li>
                    <li class="nav-item me-2">
                        <a class="nav-link" href="{{ path('app_messages') }}">
                            {% if app.user.unreadMessages|length > 0 %}
                                <i class="fas fa-envelope text-primary"></i>
                                <span class="badge bg-danger">{{ app.user.unreadMessages|length }}</span>
                            {% else %}
                                <i class="fas fa-envelope-open text-muted"></i>
                            {% endif %}
                            Messages
                        </a>
                    </li>
                    <a href="{{ path('app_logout') }}">
                        <button class="btn btn-outline-danger me-2" type="button">Logout</button>
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}">
                        <button class="btn btn-outline-success me-2" type="button">Login</button>
                    </a>
                    <a href="{{ path('app_register') }}">
                        <button class="btn btn-outline-primary me-2" type="button">Create an account</button>
                    </a>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>